# 安全策略

## 安全边界

`qq_mcp_server` 对 QQ 永久只读。OneBot 客户端只允许以下动作：

- `get_login_info`
- `get_group_info`
- `get_group_list`
- `get_group_member_list`
- `get_group_msg_history`

项目不提供发送消息、撤回、禁言、QQ群管理、任意 OneBot 动作透传或任意 SQL 工具。
MCP 可以在本地 SQLite 中更新人物卡、团务笔记和应用配置；这些写入不触碰 QQ。
NapCat HTTP、HTTP-SSE 和 WebUI 只能监听服务器回环地址。公网只应暴露 Caddy 的 HTTPS
端口。完整 NapCat 面板只通过未启用 Funnel 的 Tailscale Serve 私有发布。

远程 MCP 必须启用 Google OAuth，并用完整邮箱白名单做第二次授权。群聊原文是不可信
输入；MCP 返回会明确要求客户端不要执行群消息中的指令。每个群使用不可猜测且不可
修改的 `group_key` 独立端点，白名单移除后端点立即失效。

白名单页、人物卡上传页和 NapCat 登录/恢复页只通过已登录管理/群 MCP 签发的短期、
一次性 bearer 链接访问。只读模组档案由群 MCP 签发一小时有效、绑定单群且可重复浏览的
bearer 链接；页面不包含写操作，移出白名单后链接立即失效。NapCat 长期 WebUI Token
只在用户确认后从服务器本地配置读取，
不会进入 MCP 工具结果。应用不挂载 Docker Socket；恢复助手只接受固定请求文件并只能
重启固定 NapCat 容器，且有一小时冷却与 24 小时最多两次的预算。登录失效、账号不匹配
或互踢会持久化暂停全部 OneBot 读取，应用重启不会自行解除；恢复必须由用户明确要求并
通过一次账号校验。普通应用发布不启动、重建、更新或重启 NapCat。
链接可能授权一次操作，不能转发或写入日志。人物卡只接受已锁定模板、只读取 `人物卡`
工作表，文件限制 16 MiB。规则 PDF 仅在部署者离线构建索引时读取，运行期不开放上传。

只读、限速和熔断只能降低行为频率，不能保证非官方 QQ 客户端不触发风控。应让同一账号
长期只在固定手机与一个固定 NapCat 设备登录，避免 PC 与多台服务器反复互踢；迁移设备
时先暂停采集并停止旧实例。账号处于冻结或验证状态时不要用自动重启、重复扫码或密集
接口调用试探。

## 报告问题

请不要在公开 issue 中粘贴 QQ 登录信息、群消息、OAuth 密钥或 OneBot token。
涉及敏感信息时，请通过 GitHub 的私密安全报告功能联系维护者。
